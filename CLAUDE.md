# GND — 에이전트 작업 지침

Codex 공통 효율·소통·승인 규칙은 전역 `C:\Users\SAMSUNG\.codex\AGENTS.md`를 따른다. 이 파일은 GND의 개발 서버, 로컬 배포, 운영 DB, 검증 스크립트에만 필요한 상세 규칙이다.

---

## 개발 환경에서 먼저 확인한다 — 예외 없음 ⚠️

**모든 기능 개선은 개발 서버를 직접 실행하고 `localhost:3000`에서 실제 사용자 흐름을 확인한 뒤에만 배포한다. 개발 서버 확인을 생략한 배포는 금지한다.** 사용자 지시 (2026-07-31).

```bash
pnpm dev          # → http://localhost:3000
```

`.env.local`을 그대로 쓰므로 **운영 Supabase에 붙는다.** 즉 실데이터로 실제 화면을 볼 수 있다.

**왜 이 지침이 생겼나 — 2026-07-31에 두 번 당했다.** 0044 배포에서 lint·typecheck·단위 테스트 683건·build·번들 grep이 전부 초록이었는데, 사용자 폰에서 ① 같은 챌린지가 참가자 수만큼 **3개로 중복** 표시되고 ② **챌린지 추가 버튼이 아예 없는** 것이 드러났다. 둘 다 앱을 한 번만 띄웠으면 즉시 보였을 문제다.

**단위 테스트·빌드·번들 grep은 "화면이 어떻게 보이는가"를 하나도 검증하지 않는다.** grep은 문자열이 번들에 들어갔는지만 본다 — 그 컴포넌트가 렌더되는지, 몇 개가 렌더되는지, 눌리는지는 모른다.

### 순서

1. 마이그레이션이 있으면 **에이전트가 Supabase MCP로 직접 적용하고 객체로 검증**한다 (§DB 마이그레이션). 고위험 변경(`drop`·`delete`·`truncate`·기존 행 `update`·`revoke`·`alter default privileges` 등)은 **사용자 승인 후에** 적용한다
2. 저장소 루트에서 `pnpm dev` 실행
3. `localhost:3000`에서 **수정한 화면의 클릭·입력·이동을 직접 확인** ← 건너뛰지 않는다
4. **상대가 있어야 성립하는 기능이면 픽스처 A·B 두 계정으로 양쪽 화면을 확인** ← 아래 §사회적 기능. 사용자 지시 (2026-08-01)
5. 이상이 있으면 배포를 멈추고 수정한 뒤 2번부터 다시 확인
6. 관련 검증 스크립트 + lint·typecheck·전체 test·build
7. 검증한 코드를 로컬 `main`에 반영
8. 사용자에게 운영 배포 승인 요청
9. 승인 후 로컬 `main`의 깨끗한 복사본에서 Vercel CLI 배포
10. 프로덕션 실물 확인

**확인할 때는 "에러가 안 났다"가 아니라 "의도한 것이 보이는가"를 본다.** 목록이면 개수를 세고, 버튼이면 눌러 보고, 상태 전이면 전이시킨다. 기능을 **제거**했으면 옛 문구가 **없어졌는지**를 본다 — 새 문구만 찾으면 제거를 검증한 게 아니다.

혼자 확인하기 어려운 것(실기기 푸시)은 사용자에게 무엇을 봐야 하는지 구체적으로 적어 요청한다. **브라우저를 조작할 수단이 없는 세션이면 배포하지 말고 거기서 멈춘다** — `pnpm dev`를 띄우고 HTTP 200을 받은 것은 화면 확인이 아니다 (2026-08-01 0055에서 이걸로 한 번 샜다).

### 사회적 기능은 계정 두 개로 확인한다 — 상설 픽스처 ⚠️

**기본은 A 한 계정이다.** 대부분의 기능 개선(기록·타이머·화면 정리·설정 등)은 A로 확인하면 충분하다.

**상대가 있어야 성립하는 기능일 때만 B를 같이 쓴다** (사용자 지시 2026-08-01). 알림·크루·챌린지·응원·찌르기가 여기 해당한다 — 한 계정으로 보면 보낸 쪽만 본 것이다.

| 무엇을 고쳤나 | 확인 |
|---|---|
| 기록·휴식 타이머·피커·설정·문구·레이아웃 | **A만** |
| 알림 발송/수신, 크루 요청·수락·해제, 챌린지 초대·참가·성과, 응원, 찌르기 쿨다운 | **A + B 양쪽 화면** |

회귀 스크립트(`rls-test`·`poke-levelup-check`·`crew-link-check`·`challenge-room-check`)는 실행마다 익명 계정을 4~5개 만들어 상호작용을 검사하지만 **전부 HTTP/RPC 계층**이다. `notifications`에 행이 생긴 것까지만 본다. 받는 사람의 **알림 벨 배지·목록 문구·아이콘**, 보낸 사람의 **찌르기 버튼이 쿨다운으로 잠기는지**, 챌린지 **참가자 전용 카드가 렌더되는지**는 화면을 봐야 잡힌다.

```bash
node scripts/dev-fixture.mjs status     # 현재 상태 (읽기 전용, 기본값)
node scripts/dev-fixture.mjs create     # 없으면 만들고 크루로 상호 연결 (멱등)
node scripts/dev-fixture.mjs challenge  # 둘이 함께하는 active 챌린지 세팅 (멱등)
node scripts/dev-fixture.mjs destroy    # 픽스처 2개만 삭제
```

- 계정: `dev-테스터A`(`dev-fixture-a@gnd.local`) · `dev-테스터B`(`dev-fixture-b@gnd.local`), 크루로 연결돼 있다
- 비밀번호는 `.env.local`의 `DEV_FIXTURE_PASSWORD` (gitignore 대상, 코드에 박지 않는다)
- **크롬 = A, 엣지 = B** (또는 크롬 프로필 2개, 일반 창 + 시크릿 창). `/login`에서 각각 로그인한다
  - ⚠️ **같은 브라우저의 탭·창을 새로 여는 것으로는 안 된다.** `@supabase/ssr`의 `createBrowserClient`는 세션을 **쿠키**에 넣고, 쿠키는 브라우저 프로필 단위라 모든 탭·창이 공유한다 — **나중에 로그인한 계정으로 양쪽 다 덮인다** (2026-08-01에 이걸로 "두 창 모두 같은 크루가 보인다"고 오인했다)
  - 두 창이 정말 갈렸는지는 **⚙️ → 계정의 로그인 이메일**로 확인한다
- 익명 계정으로 하면 컨텍스트를 지우는 순간 다시 못 들어간다. 그래서 이메일 계정으로 만들었다
- ⚠️ 이 둘은 **운영 Supabase에 상주한다.** 크루 밖 사람에게는 닉네임 검색에만 보인다. `destroy`는 이메일과 닉네임을 **둘 다** 대조하고, 지운 뒤 나머지 프로필이 전부 남았는지 확인한다
- ⚠️⚠️ **사람이 픽스처 계정을 쓰기 시작하면 스크립트가 조용히 막힌다** (2026-08-31에 겪었다).
  앱에서 그 계정으로 로그인해 **비밀번호나 닉네임을 바꾸면** `.env.local`의
  `DEV_FIXTURE_PASSWORD`와 어긋나 `invalid_credentials`가 난다.
  `dev-fixture.mjs create`는 계정을 **만들 때만** 비밀번호를 넣으므로 **기존 계정은 안 고친다** —
  "create 했는데도 안 된다"가 여기서 나온다. `status`는 `auth ✅`라고만 해서 이걸 못 잡는다.
  - 고치는 법: `.env.local`의 `DEV_FIXTURE_PASSWORD`를 **실제 쓰는 값으로 맞춘다**
  - ⛔ **비밀번호를 임의로 되돌리지 마라.** 그 계정을 쓰는 사람이 못 들어간다. 먼저 물어라
  - 닉네임을 바꿨으면 `destroy`도 막힌다(닉네임 대조) — 그건 안전장치가 제대로 도는 것이다

확인 예: A가 B를 찌르기 → **B 화면 알림 벨에 뜨는가**, A의 버튼이 잠기는가 → B가 운동 완료 → **A에게 알림이 가는가**.

⚠️ **크루는 두 겹이다. 둘 다 맺어야 한다.**

| | 무엇 | 어디에 쓰이나 |
|---|---|---|
| `crew_links` | 상호 수락 그래프 (0039~) | 크루 화면의 "내 크루 N명" |
| `groups` · `group_members` | 그룹 | **홈 화면 크루 카드**, `create_challenge_room` |

링크만 맺고 그룹에 안 넣으면 한쪽 홈에 **"크루와 함께하면 더 강해져요" 빈 카드**가 뜬다. 2026-08-01에 실제로 이렇게 새어서 사용자가 화면을 보고 잡았다 — `crew_links` 1행은 멀쩡했으므로 **DB만 봐서는 못 잡는다.** `ensureGroup()`이 초대 코드로 B를 그룹에 넣는다.

⚠️ `challenge` 서브커맨드가 넘는 관문들 (하나라도 빠지면 `setup`에 머물러 챌린지 화면이 대부분 안 그려진다):
- **그룹이 먼저 있어야 한다** — `create_challenge_room`이 `no_group_yet`으로 막는다
- `start_challenge`는 joined 참가자 **전원**에게 `user_goals`를 요구한다 (`kpi_incomplete`)
- 동의도 **전원**이 해야 한다 (`consent_incomplete`) — 방장만 하면 안 된다
- `user_goals`는 setup 단계 RLS에 막혀서 `service_role`로 심는다 (회귀 스크립트와 같은 방식)

---

## 배포 — `git push`는 배포가 아니다 ⚠️

**이 프로젝트는 GitHub 연동 배포를 쓰지 않는다.** Vercel 프로젝트가 로컬 저장소에 CLI로 링크돼 있고(`.vercel/project.json`, `.gitignore`에 포함), 배포는 **검증한 로컬 `main` 코드를 기준으로 사람이나 에이전트가 로컬에서 직접 실행**해야 한다. GitHub Actions·GitHub 웹 화면·Vercel의 GitHub 자동 배포는 모두 금지한다.

```
git push   →  GitHub에만 올라간다. 사용자 폰에는 아무 변화가 없다.
vercel --prod  →  이게 실제 배포다.
```

2026-07-31에 이걸 몰라서 사고가 났다. 응원 포인트(7/29 완성)와 챌린지 종목 집계(7/30 완성)를 "배포했다"고 보고했지만 **푸시만 했고 이틀간 사용자 폰에는 옛날 코드가 돌고 있었다.** 사용자는 "기능이 적용 안 됐다"고 느꼈고, 서버 DB는 정상이라 원인을 찾는 데 오래 걸렸다.

### 배포 절차

**0. 개발 서버 확인 기록이 없으면 여기서 중단한다.** `pnpm dev`를 실행한 주소, 직접 확인한 사용자 흐름, 결과가 문서나 작업 보고에 있어야 한다. 자동 테스트와 build만 통과한 상태는 배포 가능 상태가 아니다.

**1. 배포 대상은 항상 `main`이다.** `vercel --prod`는 git 브랜치가 아니라 **현재 폴더 내용**을 그대로 올린다. 작업 브랜치에서 그냥 실행하면 미완성 코드가 프로덕션에 나간다.

배포 원본은 GitHub에서 내려받지 않는다. **현재 컴퓨터의 검증된 로컬 `main`**에서 `git archive HEAD` 등으로 `.git` 없는 깨끗한 임시 복사본을 만들고, 그 복사본에 로컬 `.vercel` 연결 정보와 필요한 환경 파일만 복사한다.

다른 세션이 같은 체크아웃에서 일하고 있을 수 있으므로 **워크트리로 분리**한다.

```bash
git worktree add /tmp/deploy-main main
cp .env.local .vercel -r /tmp/deploy-main/    # 둘 다 gitignore라 따로 복사
cd /tmp/deploy-main && npm install && npm run build
npx vercel@latest --prod --yes --scope gnd4
```

⚠️ **`--scope gnd4`를 빼면 `Not authorized`로 죽는다** (2026-08-31에 겪었다).
프로젝트가 개인이 아니라 **팀(`gnd4`) 소유**라서, 로그인은 돼 있어도(`vercel whoami`가
`coolkang292-2207`을 준다) 스코프를 안 주면 팀 프로젝트에 배포 권한이 없다고 판단한다.
`vercel teams ls`로 팀 slug을 확인할 수 있다. **같은 날 첫 배포는 스코프 없이 됐다가
두 번째에 막혔으므로, 되더라도 항상 붙여라.**

**2. 배포 후 반드시 실물로 검증한다.** "배포 명령이 성공했다"는 증거가 아니다. 프로덕션에서 파일을 직접 받아 바뀐 코드가 들어갔는지 확인한다.

```bash
curl -s https://gnd-one.vercel.app/challenge \
  | grep -oE '/_next/static/chunks/[a-zA-Z0-9._-]+\.js' | sort -u \
  | while read c; do curl -s "https://gnd-one.vercel.app$c" | grep -o "찾을문구"; done
```

**3. 프로덕션 주소는 `https://gnd-one.vercel.app`** (별칭). 배포하면 `gnd-<hash>-gnd4.vercel.app`가 생기고 이 별칭이 그쪽을 가리킨다.

**4. 마이그레이션이 있으면 순서를 지킨다.** DB 적용(**에이전트가 MCP로**, 고위험이면 승인 후) → 객체 재조회로 검증 → 배포. 앱 코드가 없는 DB 구조를 찾으면 더 크게 망가진다.

---

## 깃허브 푸시 — **배포할 때는 묻지 않고 같이 푸시한다** ⚠️

**사용자 지시 (2026-08-25 최초, 2026-08-28 추가).** **파일을 바꾼 작업**은 완료 보고를
하기 **전에** 깃허브에 푸시할지 사용자에게 묻고, "예"면 그 자리에서 푸시한 뒤 결과까지
확인하고 보고한다.

조사·설명·읽기만 한 작업은 바뀐 것이 없으므로 묻지 않는다. 묻는 규칙이
모든 답변에 붙으면 잡음이 되고, 잡음이 되면 진짜 물어야 할 때 그냥 넘어간다.

### ⛔ 배포하는 작업은 푸시를 따로 묻지 않는다 (2026-08-28)

**배포 승인을 받았으면 푸시 승인도 받은 것이다.** 묻지 말고 커밋 → 푸시 → 배포를
한 흐름으로 끝낸다.

이 저장소는 **GitHub 연동 배포를 쓰지 않아서**(위 §배포) 푸시하지 않아도 배포가 된다.
그래서 아무도 안 챙기면 **운영에는 새 코드가 도는데 깃허브는 옛 코드**로 남는다.
2026-08-28에 실제로 그럴 뻔했다 — 확대 기능을 두 번 배포하고도 원격은 `69893a1`
(시작 이미지 v5)에 머물러 있었다. 다음 세션이 원격을 읽으면 이미 살아 있는 기능을
없는 것으로 판단한다.

**배포하지 않고 끝나는 작업만 푸시 여부를 묻는다.**

### ⛔ 푸시는 배포가 아니다 — 이 규칙 때문에 더 위험해진다

매 작업마다 푸시하게 되면 **푸시해 놓고 "배포까지 끝났다"고 착각할 확률이 올라간다.**
7/29~30 사고가 정확히 그것이었다 (§배포). 그래서 이 규칙에는 짝이 되는 금지가 붙는다.

```
git push       →  GitHub에만 올라간다. 사용자 폰에는 아무 변화가 없다.
vercel --prod  →  이게 배포다.
```

**푸시했다고 완료 보고에 "배포"라고 쓰지 않는다.**

⚠️ **승인은 한쪽으로만 흐른다. 뒤집지 마라.**

| | 포함하나 |
|---|---|
| 배포 승인 → 푸시 | **포함한다.** 따로 묻지 않는다 (2026-08-28) |
| 푸시 승인 → 배포 | **포함하지 않는다.** 배포는 언제나 §배포 절차의 승인을 따로 받는다 |

푸시는 사용자 폰을 바꾸지 않고 되돌릴 수 있다. 배포는 즉시 반영된다. 무거운 쪽의
승인이 가벼운 쪽을 덮지, 그 반대는 아니다.

### 순서

검증 → **사용자 화면 확인** (§개발 환경에서 먼저 확인한다) → 커밋 →

| 배포까지 가는 작업 | 배포 없이 끝나는 작업 |
|---|---|
| **푸시**(묻지 않는다) → 푸시 결과 확인 → **배포**(§배포 절차) → 프로덕션 실물 확인 → 완료 보고 | **푸시 여부 질문** → 푸시 → 푸시 결과 확인 → 완료 보고 |

**푸시가 배포보다 먼저다.** 배포가 막혀도 원격에 코드가 남아 다음 사람이 이어갈 수
있다. 순서를 뒤집으면 "배포는 됐는데 푸시가 실패한" 상태가 되는데, 그게 이 규칙이
막으려던 바로 그 상태다.

⚠️ **릴리스 노트를 배포 전에 넣어라.** `release-notes.data.json`은 빌드 시점에
`/whats-new`로 굳는다 — 배포한 **뒤에** 항목을 추가하면 운영 `/whats-new`가 비어
있고 재배포해야 한다(2026-08-28에 이걸로 두 번 배포했다). 발송(`--send`)은 여전히
사용자가 지시할 때만 한다.

**둘 중 하나만 끝난 채로 완료 보고하지 않는다.** 푸시는 됐는데 배포가 막혔으면
"푸시함 · 배포 안 됨"이라고 그대로 쓴다.

푸시 질문이 **화면 확인과 커밋을 앞당기지 못한다.** 확인이 안 끝났으면 커밋할 것이
없으므로 물을 것도 없다. "일단 푸시해 두고 확인은 나중에"는 금지다.

### 무엇을 물어야 하나

"푸시할까요?"만 묻지 말고 **무엇이 올라가는지**를 같이 보여준다. 사용자가 판단할 수 있어야 한다.

- 커밋 몇 개가, 어느 브랜치로 (`origin`은 `https://github.com/coolkang292-design/GND.git`, 기본 브랜치 `main`)
- 미추적 파일이 남아 있으면 그 목록과 — 커밋할 것인지 그대로 둘 것인지
- 이미지·바이너리가 섞였으면 용량

### 푸시하기 전에

- **시크릿 스캔.** `git diff origin/<브랜치>..<브랜치>`에서 키 리터럴을 찾는다.
  `env.X`처럼 **읽는 코드**는 정상이고, 값이 박힌 것이 문제다
- `git add .`를 쓰지 않는다. 검증한 파일만 정확히 지정한다 (§검증 스크립트와 같은 원칙)
- 강제 푸시(`--force`)는 사용자가 명시적으로 지시할 때만

### 푸시한 뒤 — "명령이 성공했다"는 확인이 아니다

`git push`의 출력만 보고 끝내지 않는다. **다시 fetch해서 원격이 실제로 그 커밋인지** 본다.

```bash
git fetch origin --prune
git rev-list --left-right --count origin/main...main   # 0 0 이어야 한다
```

기본 브랜치가 무엇인지는 **서버에 직접 묻는다.** 로컬 `origin/HEAD`는 캐시라 늦게 따라온다.

```bash
git ls-remote --symref origin HEAD    # ref: refs/heads/main 이어야 한다
```

2026-08-25에 기본 브랜치가 `feat/cheer-points`(7/29)로 남아 있어, `main`을 올렸는데도
저장소 첫 화면이 3주 전 코드를 보여주고 있었다. 푸시는 성공했는데 보이는 것은 옛것이었다.

### 사용자가 "아니오"라고 하면

푸시하지 않고, **완료 보고에 "푸시 안 함"과 로컬에만 있는 커밋 수를 남긴다.**
말없이 넘어가면 다음 세션이 원격을 최신으로 착각한다.

---

## DB 마이그레이션

- **적용된 파일은 절대 수정하지 않는다.** 고쳐도 아무 일이 안 일어나고 다음 사람만 헷갈린다. 항상 새 번호 파일을 만든다.
- **함수의 현행 정의는 "가장 나중에 덮어쓴 파일"에 있다.** 이 저장소는 `create or replace function`으로 같은 함수를 여러 번 덮어쓴다. `start_challenge`는 0006 → 0025 → 0045 세 곳, `mark_record_beaten`은 다섯 번 덮어썼다. **파일에서 베끼지 마라.**
  - 👉 **`docs/db-current-schema.sql`을 보라.** 운영 DB의 현행 함수·정책·인덱스 전량 스냅샷이다. `pnpm db:snapshot`으로 다시 뽑는다(마이그레이션 적용 후 갱신할 것). 이게 "현행 정의"의 단일 답이다.
  - 고칠 함수가 있으면 **형제 함수도 같이 훑어라.** 2026-07-31에 `start_challenge`만 고치고 같은 전제를 공유하는 `approve_challenge_goals`를 놓쳐, 챌린지를 영영 시작할 수 없는 상태를 배포했다(0045 → 0046 → 0047로 세 번 고쳤다).
- **컬럼명을 스키마에서 확인하고 써라.** `information_schema.columns`를 먼저 조회한다. 2026-07-30에 `gm.created_at`(실제는 `joined_at`)으로 검증 24건이 연쇄 실패했다.
- **⛔ 적용은 에이전트가 한다 — 사용자에게 넘기지 마라.** 사용자 지시 (2026-08-31 최초, 2026-09-01 확정).

  > **비파괴 DB 변경은 에이전트가 Supabase MCP로 직접 적용하고 검증한다.
  > 고위험 변경은 사용자 승인 후 적용한다.**

  이 한 줄이 이 저장소의 규칙이다. 문서 다른 곳과 어긋나면 **이쪽이 이긴다** —
  §개발 환경 1번과 §배포 절차 4번도 2026-09-01에 이 문장에 맞춰 고쳤다.
  "SQL 파일을 만들었으니 SQL Editor에 붙여넣고 Run 해 주세요"로 넘기지 않는다.

  - ⚠️ **MCP가 안 붙은 세션이면 거기서 멈추고 보고한다.** 2026-09-01 배포 B 감사가
    그랬다(`.claude.json`의 `mcpServers`가 비어 있고 `supabase` CLI·`psql`도 없었다).
    그때 할 일은 **막히지 않는 조사·코드·비파괴 테스트를 전부 끝내고**, DB 변경은
    SQL 파일로만 남긴 뒤 무엇이 왜 막혔는지 쓰는 것이다. 임의 SQL 없이도
    PostgREST로 권한을 실측하는 방법이 `docs/security/public-beta-rpc-audit.md` §1에 있다
  - **적용했다 = 명령이 성공했다가 아니다.** 실행 후 **객체를 다시 조회해서** 바뀐 것을
    확인한다 (`pg_proc.proconfig` · `pg_policies` · `pg_get_functiondef` 등). 확인하지
    않았으면 `[미검증]`이라고 쓴다
  - 이 저장소는 `supabase_migrations` 이력을 쓰지 않는다(`list_migrations`가 빈 배열이다).
    **"NNNN까지 반영됐는지"는 마이그레이션 목록으로 확인할 수 없고 객체 존재로 확인한다**
  - 적용 후 **`pnpm db:snapshot`으로 `docs/db-current-schema.sql`을 갱신**한다.
    0091에서 이 단계가 빠져 저장소 스냅샷이 운영 DB보다 뒤처져 있었다(2026-08-31 발견)

- **⛔ 단, 파괴적 변경은 실행 전에 멈추고 보고한다.** 다음은 에이전트가 임의로 실행하지 않는다.
  SQL과 영향 범위를 보여주고 **사용자 승인을 받은 뒤에** 실행한다.

  | 멈춘다 | 그냥 실행한다 |
  |---|---|
  | `drop` (table·column·function·policy·trigger·index) | `create table` · `create index` · 새 함수·정책·트리거 |
  | `delete` · `truncate` · **기존 행을 바꾸는 `update`** | `create or replace function` (본문이 검증된 경우) |
  | `revoke` — RLS 헬퍼가 섞이면 정책 전체가 조용히 깨진다 | `grant` (권한을 넓히는 쪽) |
  | `alter table ... drop/alter column` | `alter table ... add column` (nullable) |
  | auth 스키마·`auth.users` 직접 조작 | 읽기 전용 조회 |

  판단이 애매하면 **멈추는 쪽**이다. 스테이징 DB가 없어서 되돌릴 곳이 없다.

- **실행 전에 `pg_temp`로 리허설할 수 있으면 한다.** 트리거 함수·순수 함수는 같은 본문을
  `pg_temp`에 복제해 임시 테이블에 걸고 돌려 보면 **운영 스키마를 건드리지 않고** 동작을
  확인할 수 있다. 2026-08-31 0092에서 이 방법으로 `search_path=''` 5개를 사전 검증했다.

- **마이그레이션 파일은 여전히 만든다.** MCP로 적용하더라도 `supabase/migrations/`에
  같은 SQL을 남긴다 — 파일이 없으면 다음 사람이 스키마의 유래를 못 읽는다.
- **Run 시점이 다른 것을 한 파일에 담지 마라.** 스테이징 DB가 없어서 `pnpm dev`도 운영 DB에 붙는다 — 즉 **개발 확인을 하려면 운영 DB에 먼저 Run해야 한다.** 그래서 "지금 돌려도 안전한 것"과 "앱 배포 뒤에 돌려야 하는 것"은 파일을 나눈다.
  - 안전: 새 테이블·인덱스·트리거·새 RPC (운영에 떠 있는 앱이 참조하지 않는다)
  - 배포 뒤: **기존 행을 바꾸는 UPDATE**, 특히 `level_definitions`의 `reward_status`를 `coming_soon` → `active`로 미는 것. 먼저 돌리면 **아직 기능이 없는 운영 앱에 "해금됨"이 즉시 뜬다** (2026-08-02 루틴에서 0056/0057로 나눈 이유)

---

## 버그 신고를 먼저 본다 (0052)

사용자가 앱에서 신고하면 `bug_reports`에 쌓이고 **관리자 폰으로 즉시 푸시**가 간다.
세션을 열면 `SessionStart` 훅이 미처리 건수를 알려준다(`.claude/settings.json`).

```bash
node scripts/bug-reports.mjs                    # 미처리(new) 전량 — 맥락·동작흔적 포함
node scripts/bug-reports.mjs --id <uuid>        # 1건 상세 (trail 전량)
node scripts/bug-reports.mjs --triage <id> --note "원인: ..."
node scripts/bug-reports.mjs --fix <id> --release <release-id>          # DRY RUN
node scripts/bug-reports.mjs --fix <id> --release <release-id> --send   # 신고자에게 알림
```

- **신고에 `build`(빌드 시각)가 찍힌다.** 며칠 묵은 번들이면 코드가 아니라 **배포가
  안 된 것**이다 — 7/29~30 사고가 그거였다. 원인을 코드에서 찾기 전에 이걸 먼저 봐라
- **`trail`이 "무엇을 하다가"를 말해준다.** 최신이 앞이다. `FAIL db POST rpc/x 400`
  같은 줄이 있으면 거기서 시작하라
- **`--send`는 사용자가 지시할 때만.** 기본은 DRY RUN이다(릴리스 공지와 같은 규약)
- 고친 뒤 `--fix`로 닫으면 신고자 폰에 "고쳤어요"가 간다. **release-notes.data.json에
  항목이 먼저 있어야 한다** — 없는 id를 넣으면 스크립트가 막는다
- 설계·근거: `docs/superpowers/specs/2026-07-31-bug-report-pipeline-design.md`

---

## 검증 스크립트 — 프로덕션에 직접 붙는다

스테이징 DB가 **없다.** `scripts/*.mjs`는 실사용자 데이터베이스에 붙는다.

- 계정 삭제는 **반드시** `scripts/_safe-delete.mjs`의 가드를 경유한다. 실행 시작 시점 스냅샷에 있는 계정은 어떤 경로로도 지워지지 않는다
- 가드는 **첫 `anonUser()`보다 앞에서** 만든다. 뒤에서 만들면 자기 픽스처가 "기존 계정"으로 잡혀 정리가 통째로 거부된다
- 정리는 **그룹 먼저, 유저 나중**. `groups.owner_id`는 `on delete cascade`가 아니라서 그룹이 남으면 방장 계정 삭제가 500으로 실패한다
- `process.exit`은 `finally` **밖**에 둔다. 안에 두면 예외를 삼켜 아무것도 검증 못 한 실행이 exit 0으로 보고된다
- 연달아 돌리면 익명 가입 rate limit(429)에 걸린다. 사이에 1~2분 둔다
- 실행 후 계정 수를 확인한다. **기준은 프로필 4개** — 오뎅끼데스까·스칼레또·낭만송곳니·repro-mry7tyx0. 이 4개가 아니면 픽스처가 남은 것이다.
  - **인증 계정 총수는 기준선으로 쓰지 마라.** 개발 서버를 열 때마다 익명 계정이 하나씩 생겨 계속 늘어난다(2026-07-31 하루에 6개). 프로필 없는 익명 계정은 앱에서 안 보이고 해가 없다. 다만 **지우지 마라** — 사용자 폰이나 브라우저의 로그인 세션일 수 있어 지우면 그 기기에서 로그아웃된다.
  - 2026-07-31에 테스트 계정 `테스트1`·`테스트11`·`형`을 사용자 지시로 삭제했다. 삭제는 **닉네임 허용목록 + 개수 가드**를 건 스크립트로만 한다. 그룹을 먼저 지워야 방장 계정이 지워진다.

### 회귀 기준선 — 전부 `0 failed`

**기준선은 이 문서가 아니라 `scripts/regression-baselines.json`이 갖는다.**
표를 손으로 고치던 시절에 두 번 어긋났다 — 2026-08-01 이전 표(113·11·20·32)는
단언이 늘어난 뒤로도 안 고쳐져 통과인데 "기준보다 많다"고 헷갈리게 만들었고,
표에 6종만 있어서 나머지 23종은 기대값이 아예 없었다. 기계가 읽고 기계가 갱신한다.

```bash
pnpm verify:regression                  # core 6종 — 배포 전 기본 루틴
pnpm verify:regression --tier readonly  # 계정 안 만드는 5종 — 빠르고 안전
pnpm verify:regression --list           # 30종 전량 목록과 현재 기준선
pnpm verify:regression --all            # 전량 ⚠️ 30분+ (익명 가입 대기 포함)
pnpm verify:regression --only rls-test --record   # 측정값으로 기준선 갱신
```

**판정 규칙** (러너가 강제한다):

| 결과 | 뜻 |
|---|---|
| `failed > 0` | 실패. **예외 없다.** |
| `passed < 기준선` | 실패 — 단언이 사라진 것이므로 회귀다 |
| `passed > 기준선` | 경고 — 단언이 늘었으니 `--record`로 갱신하라 |

⚠️ **러너는 종료 코드를 믿지 않는다.** 요약줄의 `passed`/`failed`를 파싱해 기준선과
대조한다. 단언이 통째로 사라져 "0 failed"가 된 경우는 종료 코드로 안 잡히기 때문이다
(§테스트가 진짜 테스트인지 확인한다).

⚠️ `tier: accounts`인 23종은 실행마다 **운영 Supabase에 익명 계정을 만든다.**
러너가 사이에 90초씩 대기한다(429 회피). 배포 직전이 아니면 `--tier readonly`로 충분하다.

⚠️ `tier: fixture`가 2종이다(`peek-reset-check` · `block-report-goal-check`) — 먼저
`node scripts/dev-fixture.mjs create && node scripts/dev-fixture.mjs challenge`.

⚠️ **`block-report-goal-check`는 운영 DB에 쓴다**(차단·신고·모집 토글). 전부
되돌리고 되돌렸는지까지 단언하지만, 중간에 죽으면 픽스처 A가 B를 차단한 채로
남을 수 있다 — 그때는 다시 돌리면 정리된다. 이 스크립트가 필요한 이유는
**차단이 `is_crew_with` 한 함수에 얹혀 있어서**다. 그 함수를 손대면 피드·댓글·
응원·프로필·콕찌르기·기록열람·크루목록·검색·모집글 아홉 곳이 동시에 풀리는데,
화면으로는 "안 보여야 할 것이 안 보인다"를 확인하기 어렵다(부정 확인이다).
0090의 트리거 두 개도 **REST로도 스키마 스냅샷으로도 안 보인다** — 실제로
목표를 낮춰 보는 것이 유일한 확인이다.

**기준선의 신뢰도는 `source` 필드가 말한다.** `measured`(9종)는 실측이고,
`derived`(17종)는 `check()` 호출을 정적으로 센 **±1 추정**이다 — 처음 돌릴 때
`--record`로 확정하면 된다. `no-count`(4종)는 단언 카운터가 없어 종료 코드만 본다
(`streak-parity-check`·`badge-metrics-check`·`official-program-catalog-check`·`cardio-xp-check`
— 카운터를 붙이는 것이 남은 일이다).

**실패가 하나라도 있으면 회귀다.** 예전엔 "원래 6건 실패"인 상태라 진짜 고장을 가렸다.
2026-08-31 기준 **`knownFailure`는 0건이고 `--tier readonly`는 5종 전부 통과한다.**
알려진 실패를 다시 만들지 마라 — 남겨 두면 그것이 진짜 회귀를 가린다.

마지막 두 건을 고치면서 배운 것: **`challenge-aggregation-parity`가 지키던 대상이
틀려 있었다.** 0044에서 집계를 `group_id` → 참가자로 전환했는데 단언은 여전히
"참가자 집합 == 그룹원 집합"을 전제해서, **아무도 안 부르는 죽은 경로를 지키면서
정상 동작을 실패로 신고**하고 있었다. 공개 모집(0085~0087)이 열리자 참가자가
그룹원이 아닌 경우가 정상이 되어 반대 방향으로도 깨졌다. 지금은 운영이 실제로 쓰는
`get_challenge_period_sessions` RPC를 대조한다. ⚠️ **회귀 스크립트를 고칠 때는
"단언이 통과하는가"가 아니라 "지금도 운영이 그 경로를 쓰는가"를 먼저 물어라.**

⚠️ **이건 RPC·DB 계층 검증이다. 화면 확인을 대신하지 않는다** (§개발 환경에서 먼저 확인한다).

---

## 테스트가 진짜 테스트인지 확인한다

이 저장소에서 실제로 나온 가짜 통과들이다. 같은 걸 만들지 마라.

- `check("...", true, "")` — 정답을 박아둔 단언. 무슨 일이 있어도 통과한다
- `process.exit`이 `finally` 안 — 크래시가 성공으로 보고된다
- "0이어야 한다"는 단언 — 서버가 완전히 망가져도 0이라 통과한다. **"10이어야 한다"로 뒤집어야** 고장이 잡힌다
- 단언 0건 — 픽스처가 비면 아무것도 검사하지 않고 통과한다. 개수 가드를 넣어라

**기준: 일부러 고장냈을 때 그 단언이 실패하는가.**

---

## 같은 사실을 두 곳에 두지 않는다

코드를 고쳤으면 **계획서·인수인계서·`PROGRESS.md`도 같이** 고친다. 갈라지면 다음 사람이 틀린 쪽을 믿는다.

- 상수를 바꾸면 그 값이 박힌 **화면 문구**도 찾아 고친다 (예: `setup-sheet.tsx`의 "+3점")
- 문서가 `파일:줄번호`를 가리키면 그 줄이 밀렸는지 확인한다
- 원본 주석을 요약해 옮기지 마라. 회귀를 잡는 테스트 위치가 주석에 박혀 있다
