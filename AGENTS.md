# GND 공통 작업 규칙

이 파일은 Claude가 아닌 에이전트도 반드시 읽는 공통 입구다. 상세 규칙은 `CLAUDE.md`, 현재 상태는 `PROGRESS.md`, 작업별 최신 사실은 가장 최근의 관련 `docs/superpowers/HANDOFF-*.md`에 있다.

## 작업 시작 순서

1. `git rev-parse --show-toplevel`, `git status --short`, `git log -1 --oneline`으로 실제 저장소·사용자 작업·기준 커밋을 확인한다.
2. `CLAUDE.md` → `PROGRESS.md` → 가장 최근의 관련 인수인계서 → 진행 중인 설계서·계획서 순서로 읽는다.
3. 문서만 믿지 말고 실제 코드, 호출부, 테스트, 필요하면 `docs/db-current-schema.sql`을 확인한다.
4. 시작 전에 `[확실]`, `[미검증]`, 이번에 바꿀 범위와 바꾸지 않을 범위를 짧게 적는다.

## 작업 중 규칙

- 사용자의 기존 수정과 추적되지 않은 파일은 손대지 않는다. `git add .`를 쓰지 않고 검증한 파일만 지정한다.
- 기존 구조를 유지하고 필요한 부분만 고친다. 함수의 뜻이나 입력값을 바꾸면 형제 함수와 모든 호출부를 함께 찾는다.
- 변경 중에는 관련 작은 테스트만 반복하고, `lint`·`typecheck`·전체 `test`·`build`는 마무리에서 한 번 실행한다.
- 작은 작업에 불필요한 전체 재탐색, 같은 내용의 반복 검토, 여러 에이전트 호출을 붙이지 않는다.
- 이 프로젝트의 `.env.local`과 DB 검증 스크립트는 운영 Supabase에 연결된다. 계정 생성·삭제나 실제 데이터 변경 가능성을 먼저 확인한다.
- 오류를 숨기는 문구만 바꾸지 말고, 증상 → 직접 원인 → 내부 원리 → 수정 → 재발 방지 테스트 순서로 처리한다.

## 배포 전 필수 관문 — 순서를 바꾸지 않는다

1. 개발 서버(내 컴퓨터에서만 여는 시험용 앱): 저장소 루트에서 `pnpm dev`를 실행한다.
2. `http://localhost:3000`에서 수정한 화면을 실제로 열고, 사용자가 하게 될 클릭·입력·이동을 직접 확인한다.
3. 이상이 있으면 배포하지 않고 수정한 뒤 개발 서버 확인부터 다시 한다.
4. 화면이 정상일 때만 `lint`·`typecheck`·전체 `test`·`build`를 실행한다.
5. 모든 확인이 끝나도 바로 배포하지 않고 사용자에게 운영 배포 승인을 따로 요청한다.
6. 승인받은 뒤 **검증한 로컬 저장소의 `main` 코드만** Git 없는 깨끗한 임시 복사본으로 만들어 Vercel CLI의 `vercel --prod`로 배포한다.
7. GitHub Actions, GitHub 연동, GitHub 웹 화면은 운영 배포에 사용하지 않는다. `git push` 성공을 배포 성공으로 보고하지 않는다.
8. 배포 뒤 Vercel `Ready`, `gnd-one.vercel.app` 별칭, 주요 경로 응답, 실제 운영 파일의 변경 내용을 확인한다.

## 반드시 따로 승인받는 일

- 운영 배포, DB 적용·초기화, 중요 데이터 삭제, 비밀값 변경, 실제 사용자 알림 발송, 복구하기 어려운 Git 변경
- 운영 배포는 위 필수 관문을 전부 통과한 로컬 `main`에서 Vercel CLI로만 한다. GitHub 푸시나 GitHub 연동 배포를 사용하지 않는다.
- 업데이트 공지는 사용자가 지시했을 때만 보낸다.

## 완료 조건

1. 관련 테스트와 `lint`·`typecheck`·전체 `test`·`build` 결과를 직접 확인한다.
2. 화면 변경은 로컬 앱에서 실제 클릭 흐름을 확인한다.
3. `PROGRESS.md`와 최신 인수인계서에 수정 내용, 커밋, 테스트 수, 배포 여부, 폰/미검증 항목, 다음 할 일 1개를 기록한다.
4. 배포하지 않았으면 `운영 배포 대기`라고 분명히 쓴다. 폰에서 확인하지 않았으면 완료로 꾸미지 않는다.

## 사용자 설명

사용자는 비개발자다. 결론부터 한국어로 설명하고, 전문용어는 `전문용어(쉬운 말): 초등학생도 이해할 한 문장` 형식으로 바로 풀어 쓴다.
