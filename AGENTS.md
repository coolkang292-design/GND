# GND 프로젝트 작업 규칙

Codex 공통 효율·소통·승인 규칙은 `C:\Users\SAMSUNG\.codex\AGENTS.md`를 따른다.

⚠️ **이 파일은 `CLAUDE.md`와 같은 사실을 두 번 적지 않는다.**
개발 환경 확인·배포 절차·DB·검증 스크립트 규칙은 **`CLAUDE.md`가 단일 원천**이고,
여기에는 거기 없는 것만 남긴다. 예전에 이 파일이 배포 절차를 따로 적고 있다가
2026-08-01에 추가된 **두 계정 검증**과 **마이그레이션 선행 적용**을 놓친 채로
2026-08-19까지 남아 있었다 — 갈라진 쪽을 읽은 에이전트는 절반만 확인하고 배포한다.
(`CLAUDE.md` §같은 사실을 두 곳에 두지 않는다)

## 작업 전에 읽을 것 — `CLAUDE.md`

| 무엇 | 어디 |
|---|---|
| 개발 서버에서 화면을 눈으로 확인 (예외 없음) | `CLAUDE.md` §개발 환경에서 먼저 확인한다 |
| 사회적 기능은 픽스처 A·B **두 계정**으로 양쪽 화면 | `CLAUDE.md` §사회적 기능은 계정 두 개로 확인한다 |
| 배포 절차 — `git push`는 배포가 아니다 | `CLAUDE.md` §배포 / §배포 절차 |
| 마이그레이션 작성·적용 순서, 현행 함수 정의를 찾는 법 | `CLAUDE.md` §DB 마이그레이션 |
| 운영 DB에 붙는 스크립트, 계정 삭제 가드 | `CLAUDE.md` §검증 스크립트 |
| 회귀 기준선 — 전부 `0 failed` | `CLAUDE.md` §회귀 기준선 |
| 버그 신고 확인 | `CLAUDE.md` §버그 신고를 먼저 본다 |

**요약하지 말고 그 파일을 열어라.** 여기 옮겨 적는 순간 또 갈라진다.

## 시작 순서

1. `git rev-parse --show-toplevel`, `git status --short`, `git log -1 --oneline`으로 실제 저장소와 현재 변경을 확인한다.
2. `CLAUDE.md` → `PROGRESS.md` → 최신 관련 `docs/superpowers/HANDOFF-*.md` → 진행 중인 설계·계획 문서 순서로 읽는다.
3. 문서만 믿지 말고 실제 코드·테스트·필요하면 `docs/db-current-schema.sql`을 확인한다.
   - ⚠️ `docs/superpowers/plans/archive/`(44건)는 **실행이 끝난 계획서**다. 검색에 먼저 걸리더라도 현행 사실이 아니다.
     `plans/`에 남은 4건만 진행 중이다. **빈 체크박스를 미완료로 읽지 마라** — 실행하면서 표시하지 않았을 뿐이다.
     현행 사실은 코드와 `docs/db-current-schema.sql`이다. 자세히는 `docs/superpowers/plans/README.md`.
   - ⚠️ `.worktrees/` 아래에는 다른 브랜치의 사본이 있다. `find`·`grep`이 그쪽 파일을 물어 오면 현재 코드로 착각하지 않는다.

## 종료 기록

- 작업 마지막에만 `PROGRESS.md`와 최신 인수인계서를 한 번 갱신한다.
- 코드 커밋, 실제 검사 수치, DB 적용 여부, 배포 여부, 사용자 기기 미확인 사항, 다음 할 일 1개를 남긴다.
- 검증한 파일만 정확히 지정해 스테이징한다. `git add .`를 사용하지 않는다.
- 업데이트 공지(`pnpm release:notify`)와 버그 신고 알림(`bug-reports.mjs --send`)은 **사용자가 지시했을 때만** 보낸다. 기본은 DRY RUN이다.
