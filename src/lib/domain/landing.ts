/**
 * **앱을 켰을 때 떨어지는 자리** — 단일 원천 (2026-08-19 사장님 결정).
 *
 * 옛 값은 `/home`이었고 **여섯 파일에 흩어져** 있었다. 그중 하나가
 * `manifest.ts`의 `start_url`인데, 폰 홈화면에 설치한 사람은 **그 값으로만**
 * 들어온다 — `app/page.tsx`의 리다이렉트를 아예 안 탄다. 흩어진 채로 바꾸면
 * 그 한 곳이 반드시 남고, **설치해서 쓰는 사람만 옛 화면을 본다.**
 *
 * ⚠️⚠️ **여기 말고 다른 곳에 랜딩 주소를 적지 마라.**
 *    `landing-usage.test.ts`가 소스를 훑어 위반을 막는다.
 *
 * ── 왜 `/record`인가 ────────────────────────────────────────
 * 사용자 요청: *"어플 시작을 하면 바로 기록 메뉴로 랜딩"*.
 *
 * ⚠️ **"기록 화면이 무거워서 첫 화면이 느려진다"는 걱정은 실측으로 기각됐다**
 *    (2026-08-19, 프로덕션 빌드 · 콜드 캐시 · CPU 6배 감속):
 *      JS 전송 377KB로 **동일** · DOM 노드 92 대 245(홈) · FCP 720ms 대 880ms(홈).
 *    `/record`가 오히려 가볍다. 줄 수(3,133줄)는 무게가 아니다 — 18%가 주석이고
 *    나머지도 대부분 버튼을 눌러야 도는 핸들러다.
 *
 * ── 여기로 보내면 **안 되는** 두 경우 ───────────────────────
 * "모든 입구"는 정확히는 **의도가 없는 입구**다. 의도를 갖고 온 사람은 그 의도로 보낸다.
 *   1. **푸시 알림 클릭** — `domain/push.ts`가 유형별 목적지를 갖고 있다.
 *      찌르기 알림을 눌렀는데 기록 화면이 나오면 알림이 망가진다.
 *   2. **초대를 들고 온 로그인/온보딩** — `/challenge?join=…`으로 가야 한다.
 *      기록으로 보내면 **초대가 조용히 사라진다**(2026-08-08 주석의 사고).
 */
export const APP_LANDING_PATH = "/record";

/**
 * **운동을 마치고 「확인」을 눌렀을 때** 가는 자리 (2026-08-19 사장님 결정).
 *
 * 랜딩과 **반대 방향**이다. 켤 때는 운동하러 가고, 마치면 사람을 보러 간다.
 *
 * ⚠️ 이 이동의 근거는 "효율"이 아니라 **그 순간에만 열리는 기능**이다:
 *    찌르기는 `iWorkedOut`일 때만 눌린다(`friend-board-card.tsx:234`). 그리고
 *    *"오늘 운동을 마치면 크루를 콕 찌를 수 있어요 👉"* 안내는 **홈에만** 있다.
 *    옛 판은 운동을 마치면 **빈 기록 화면**에 남아서, 방금 열린 권한을 아무도
 *    못 봤다.
 *
 * ⚠️⚠️ **운동 완료 즉시 보내지 마라. 「확인」을 누른 뒤다.**
 *    완료 화면에는 인증사진(진짜 유저 세션의 **93%**가 붙인다) · 기록 갱신 ·
 *    챌린지 기여 · XP 결과가 함께 있다. 사진 업로드를 이동 트리거로 걸면
 *    사진 올린 사람은 나머지를 못 보고, 안 올리는 사람은 영영 안 넘어간다.
 */
export const AFTER_WORKOUT_PATH = "/home";
