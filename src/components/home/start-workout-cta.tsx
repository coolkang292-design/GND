import Link from "next/link";
import { UiIcon } from "@/components/ui-icon";

/**
 * 금색 단일 `운동 시작하기` 버튼.
 *
 * ⚠️⚠️ **2026-08-21 현재 호출부가 없다.** 홈의 주 행동 버튼은
 * `personal-today-card.tsx`로 옮겨 갔고, 거기서는 오늘 상태에 따라 문구가
 * `오늘 운동하고 +N XP` / `운동 이어가기` / 칭찬 배너로 갈린다(설계 §6.2).
 * 이 파일을 홈에 되살리면 **주 행동 버튼이 둘**이 된다. 아래 옛 설명은 2026-08-13~
 * 08-20의 배치를 적은 것이다.
 *
 * (옛 설명) 홈의 유일한 주 행동 버튼 (2026-08-13 개편).
 *
 * 설계: `docs/superpowers/specs/2026-08-13-home-today-card-and-challenge-cta-design.md`
 *
 * ⚠️ **친구 카드 안으로 들어왔다.** 옛 자리는 홈 최상단의 단독 블록이었는데,
 * "운동할 이유"(친구가 오늘 했는가)와 "운동하는 수단" 사이에 카드가 5개 끼어
 * 있었다. 이유를 본 순간 누를 것이 있어야 한다는 것이 이 개편의 전부다.
 *
 * ⚠️ **홈에 이 버튼이 두 개가 되지 않게 하라.** 옛 블록을 되살리면 같은 화면에
 * `운동 시작하기`가 둘이 된다.
 *
 * ⚠️ 문구가 옛 3줄(`오늘의 운동` / `운동 시작하기` / `30초면 기록할 수 있어요.
 * 친구들이 기다리고 있어요.`)에서 줄었다. `친구들이 기다리고 있어요`는 **바로 위
 * 목록이 이미 말한다** — 그리고 줄인 높이가 그대로 접힘선 여유가 된다(설계 §6).
 *
 * ⚠️ **단색 금색이다** (2026-08-13 사용자 지시 "운동시작하기도 금색으로"). 옛 배경은
 * `from-accent to-[#0B6E66]`(금색 → 청록) 그라데이션이라 화면에서 **올리브색**으로
 * 읽혔다. 홈에서 가장 먼저 눌러야 하는 것이므로 앱의 주 강조색을 그대로 쓴다.
 */
export function StartWorkoutCta() {
  return (
    /* ⚠️ **가운데 정렬 + 불꽃**이다 (2026-08-13 사용자 지시, 시안 첨부). 옛 배치는
       왼쪽에 `운동 시작하기`, 오른쪽에 `30초면 기록돼요 ›`였다. 보조 문구를 지운
       것은 버림이 아니라 **폭을 비운 것**이다 — 아이콘과 글자가 가운데 모여야
       버튼으로 읽힌다.
       ⚠️ 불꽃은 이모지가 아니라 `streak-on` 아이콘이다. 이 앱은 2026-08-07에 홈의
       이모지를 이 아이콘 세트로 갈아치웠다 — 여기만 이모지를 쓰면 혼자 튄다. */
    <Link
      href="/record"
      className="mt-3 flex h-12 items-center justify-center gap-1.5 rounded-card bg-accent text-[15px] font-extrabold text-accent-ink shadow-card"
    >
      <UiIcon name="streak-on" size={18} />
      운동 시작하기
    </Link>
  );
}
