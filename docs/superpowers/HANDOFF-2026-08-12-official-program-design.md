# GND 공식 추천 프로그램 설계 인수인계

## 현재 상태

공식 프로그램 5종의 정적 카탈로그 구현과 실제 운동 카탈로그 이름 검증까지 완료했다.
아직 일정 등록·운동 추가 UI·DB 마이그레이션은 구현하거나 적용하지 않았다.

- 프로그램: 어깨·가슴·팔·하체·체지방 관리 **5종**, 각 A/B/C로 총 **15회차**
- 처방: 어깨 16개·가슴 15개·팔 16개·하체 15개·체지방 관리 15개, 총 **77개**
- 실제 카탈로그 계약: 고유 운동명 **27개**, 읽기 전용 검사 **27/27 통과**
- 구현 커밋: `cbcdfab` → `e5d115d`
- 품질 보강: 버전·이미지 경로·전체 처방값 스냅샷, 공식 휴식시간 타입 경계,
  이름 있는 처방 인자, 입력 불변성, 누락 이름 순서, 시드 종목 판정과 공백 정규화

- 설계: `specs/2026-08-12-official-workout-programs-design.md`
- 통합 UI 설계: `specs/2026-08-12-recommendation-hub-cognitive-layout-design.md`
- 1단계: `plans/2026-08-12-official-program-scheduling.md`
- 2단계: `plans/2026-08-12-program-guided-workout.md`
- 3단계: `plans/2026-08-12-exercise-guides-and-source-links.md`
- 5종 카탈로그 확장: `plans/2026-08-12-five-official-program-catalog.md`
- 통합 운동 추가 UI: `plans/2026-08-12-exercise-entry-hub-and-program-ui.md`

## 확정된 핵심

- GND 공식 프로그램만 제공한다. 개인 `workout_routines`와 분리한다.
- 첫 프로그램은 `시선이 머무는 어깨` / `상체의 틀을 넓히는 6주`다.
- 주 3회 고정, 6주 18회, 시작 날짜와 요일별 시간을 사용자가 고른다.
- 충돌·결석은 기존 계획을 자동 삭제하지 않고 미리보기와 사용자 확인을 거친다.
- 최근 성공 무게 자동 입력, 종목별 반복 범위·휴식, 첫·마지막 세트 피드백을 쓴다.
- 운동 지침은 GND 안내가 본체이고 네이버 원문은 검수된 선택 링크다.
- 운동 추가 첫 화면은 `프로그램으로 시작하기`와 `운동 직접 고르기`를 병행한다.
- 프로그램 목표는 어깨·가슴·팔·하체·다이어트 5종이며 전신 기본 운동을 유지한다.
- 상황별·부위별 추천은 검색 아래, 지난 운동·내 루틴·인터벌은 빠른 시작으로 유지한다.
- 사용자 노출명은 `4분부터 시작하는 전신 인터벌`이며 내부 `tabata`는 바꾸지 않는다.
- 일정 UI는 시작일 → 요일·시간 → 18회 미리보기 세 단계다.

## 실행 순서와 게이트

1. 카탈로그 Task 1~4는 완료했다. 기존 일정 계획의 Task 1은 다시 실행하지 않는다.
2. 다음은 `plans/2026-08-12-official-program-scheduling.md`의 Task 2~5다.
3. 0066은 사용자가 SQL Editor에서 직접 Run해야 한다.
4. 개발 서버에서 18개 계획·충돌·재배치·일반 계획 회귀를 직접 조작한다.
5. 2단계의 0067도 사용자 SQL 적용 뒤 실 DB 검사를 실행한다.
6. 3단계는 DB 없이 구현하되 외부 링크를 추측해 추가하지 않는다.
7. 각 단계마다 lint·typecheck·전체 test·build 후 사용자 승인 전에는 배포하지 않는다.

## 카탈로그 단계 검증

- `pnpm test -- src/lib/domain/official-programs.test.ts src/lib/domain/workout-plan.test.ts`:
  **2파일·28건 통과**
- `pnpm typecheck`: 오류 0건
- `pnpm programs:check-catalog`: **PASS 27/27 official-program exercise names**
- `git diff --check`: 출력 0줄
- 실제 DB 작업: 읽기 전용 GET만 실행, 쓰기 **0건**, 마이그레이션 적용 **0건**
- **[미검증]** 개발 서버 UI 확인은 아직 실행하지 않았다. 운영 배포도 실행하지 않았다.
  화면 구현 뒤 직접 조작 확인 → 전체 검사 → 사용자 승인 순서를 거쳐야 한다.

다음 할 일은 기존 일정 계획 Task 2~5다. 그 뒤 통합 운동 추가 UI 계획, 기존 일정
계획 Task 7~8 순서로 진행한다. 처방이 비어 있는 `준비 중` 카드는 운영에 노출하지
않는다.
