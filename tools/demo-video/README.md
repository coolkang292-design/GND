# GND 인플루언서 데모 영상 도구

운영 중인 GND를 **픽스처 계정 두 개로 실제로 조작하며** 휴대폰 세로 화면(1080×1920)으로 녹화하고,
장면 표시대로 잘라 붙여 1분짜리 데모 영상을 만든다. **앱 코드는 한 줄도 바꾸지 않는다.**

> 원칙 (사용자 지시): 실제 GND 화면만 쓴다. 가짜 UI·목업·AI 생성 장면·과한 전환 효과는 쓰지 않는다.
> 편집은 컷과 짧은 자막, 마지막 문구뿐이다.

## 산출물 (2026-09-14 녹화)

`artifacts/`에 있고, 이 폴더는 **git에 올리지 않는다**(용량).

| 파일 | 길이 | 내용 |
|---|---|---|
| **`gnd-influencer-first-contact-shorts.mp4`** | 59.3초 | **유튜브 쇼츠용 (2026-09-15)** — v2 장면 + 운동 완료 뒤 **피드 댓글 3컷**(A가 B 게시물에 댓글 → B 알림 → B 답글, 1.75배속). `layout: "shorts"`: 자막 카드를 위(290~480), 앱 화면 790 폭을 그 아래에. 사용자 스크린샷에서 잰 유튜브 UI(위 0~280 · 아래 1480~ · 오른쪽 x900~/y950~1480)를 자막이 피한다. 앱 화면 아래 약 30%는 유튜브 제목에 가린다(사용자가 고른 안 B — 화면을 줄이면 글자가 너무 작다). 댓글 장면은 `work/rec/comment-20260915` (`edit-plan-first-contact-shorts.json`) |
| **`gnd-influencer-first-contact-50s-v2.mp4`** | 50.0초 | **첫 접촉용 v2 (2026-09-15)** — v1과 장면·길이·효과음·손가락이 같고, 자막만 쉬운 판과 같은 **흰 카드(큰 제목 + 작은 설명)** 로 바꿨다(사용자: "50초 자막이 잘 안 보인다"). 단계 번호 없음, 첫 6초는 헤드라인이 카드 제목. 마지막 화면 "팔로워와 첫 GND 챌린지를 함께 테스트해보세요" (`edit-plan-first-contact-v2.json`) |
| `gnd-influencer-first-contact-50s.mp4` | 50.0초 | 첫 접촉용 v1 (보존) — 앱 화면 90% + 작은 알약 자막, 마지막 "혼자였다면…/GND Beta/CTA" (`edit-plan-first-contact.json`) |
| **`gnd-influencer-demo-easy.mp4`** | 97.1초 | **쉬운 판** — 둥근 앱 화면 + 아래 흰 카드 자막(골드 테두리·그림자), ①~⑨ 단계, 핵심 순간 멈춤, 손가락 18개, 배경음 (`edit-plan-easy.json`). 2026-09-15 사용자 지시로 다시 만듦: 마지막 문구 → "팔로워와 첫 GND 챌린지를 함께 테스트해보세요", 사진 자막 "사진 한 장으로" → "⑥ 운동하면서 사진으로 인증 / 세트 사이에 탭 한 번 · 최대 5장"(운동 중 최대 5장, `MAX_WORKOUT_PHOTOS`). SHA-1 `763e116a…`. 이전 판(`4406e5f2…`)은 `artifacts/backup-2026-09-15/` |
| `gnd-influencer-demo-easy-nocaption.mp4` | 93.1초 | 쉬운 판 이전 배치(자막 띠)의 자막 없는 판 — 카드·손가락·음악 적용 전 |
| `gnd-influencer-demo-60s.mp4` | 59.4초 | 자막 + 마지막 문구 |
| `gnd-influencer-demo-60s-nocaption.mp4` | 55.9초 | 자막·문구 없음(화면만) |
| `gnd-influencer-demo-raw-B.mp4` | 약 27분 | B 화면 원본(자르지 않음, 대기 시간 포함) |
| `gnd-influencer-demo-raw-A.mp4` | 약 28분 | A 화면 원본 |

- 해상도 1080×1920 · 30fps · H.264 MP4
- 촬영 대상: **운영 `https://gnd-one.vercel.app`** (그날 운영 빌드 = `main` `7d82645`와 같은 코드. 운영 번들의 빌드 시각 09-11 15:10이 마지막 커밋 15:06 뒤)
- 녹화 원본 프레임: `work/rec/final-20260914/{A,B}/` (gitignore)

## 사용 계정

| | 계정 | 역할 |
|---|---|---|
| A | `dev-fixture-a@gnd.local` (헬스장주주) | 챌린지를 만들고, 먼저 운동하고, 응원·좋아요를 보내는 친구 |
| B | `dev-fixture-b@gnd.local` (근육은퇴근중) | 링크로 참가해 운동·사진 인증을 하는 주인공 |

- 비밀번호는 이 도구가 **모른다.** `login.mjs`가 연 창에서 **사람이 직접** 로그인하고, 세션은
  `.profiles/<계정>/` 브라우저 프로필에 남는다(gitignore — **절대 커밋하지 않는다**).
- `@gnd.local` 계정은 `src/lib/domain/analytics-accounts.ts`가 이미 관리자 통계에서 뺀다.

## 장면 구성 — 각 장면의 실제 기능

| # | 화면 | 실제 기능 | 자막 |
|---|---|---|---|
| 1 | A | 챌린지 탭 → ＋ 챌린지 추가하기 → 이름 입력 → 목표(웨이트 운동일 하루 1종목+·주 3일)·참여 계획 주 3일 → 만들기 | 친구와 챌린지를 만들고 |
| 2 | A | 🔗 초대 링크 복사하기 → 링크 표시 | 〃 |
| 3 | B | 초대 링크 열기 → "같이 하면 한다 챌린지에 참가했어요!" | 〃 |
| 4 | B | 홈: 운동 전 · 스트릭 소멸 경고 · 오늘의 크루 · 9월 4주 챌린지 48% | 혼자였으면 오늘도 미뤘을 운동 |
| 5 | B | 피드: A의 트레드밀·벤치프레스 완료 기록(인증 사진·33분·3.6km) | 친구가 운동한 걸 보고 |
| 6 | B | 기록 탭 → 운동 시작 | 나도 시작 |
| 7 | B | 벤치프레스 무게 + 두 번(55→60kg) → 1세트 완료 → 휴식 타이머 | 운동 기록 + 사진 인증 |
| 8 | A | 홈에서 "근육은퇴근중 1분째 운동 중" → 💪 힘내 → "응원을 보냈어요! +10 P" | 친구가 반응하고 |
| 9 | B | 휴식 중 실시간 배너 "헬스장주주님의 응원 📣" | 〃 |
| 10 | B | 운동 중 사진 버튼 → 카메라(파일 선택) → 사진 1/5 | 운동 기록 + 사진 인증 |
| 11 | B | 2세트 60kg 완료 → "오늘 계획한 운동을 다 했어요" | (없음) |
| 12 | B | 결과: 오늘 운동 완료 · 볼륨 1,200kg · 🏅 기록 갱신 40kg · 오늘의 사진 | 운동하면 기록이 쌓이고 |
| 13 | A | 피드 맨 위 B의 기록에 ❤️ 좋아요 | 친구가 반응하고 |
| 14 | B | 알림함: 반응 · 응원 · 운동 시작 알림 | 〃 |
| 15 | B | 챌린지 탭: 9월 4주 챌린지 **48% → 58%**, 종합점수 45.9 → 56.4, 챌린지 활동 | 챌린지는 계속된다 |
| 끝 | — | 마지막 컷을 어둡게 깔고 "혼자였다면 안 했을 운동을, 같이 하니까 하게 된다." · GND Beta (60초판·첫 접촉 v1). 쉬운 판·첫 접촉 v2는 "팔로워와 첫 GND 챌린지를 함께 테스트해보세요" | |

**자막을 기획안에서 바꾼 곳 두 군데**
- 1~3번 장면의 "친구와 챌린지를 만들고": 챌린지 만들기 장면을 추가하면서 넣었다.
- 12번 "운동하면 **기록**이 쌓이고"(원문: XP가 쌓이고): B가 2세트만 해서 XP가 지급되지 않았다(아래 §XP).

**영상에 넣지 않은 것**
- 새 챌린지의 "지금 바로 시작하기": 눌러도 오늘 운동이 집계되지 않는 문제가 있어(§발견한 문제 1) 누르지 않았다. 챌린지는 9월 15일 자동 시작.
- A의 오후 기록(침실·기내 사진, AI 연출 이미지로 보임)은 피드에서 스크롤하지 않아 화면에 거의 나오지 않는다.

## 인증 사진

- A(러닝머신 계기판 34:12·3.60km), B(스마트워치 인터벌 기록): **운영자 본인 계정(오뎅끼데스까)이 실제로 찍은 사진**을
  사용자 허락을 받아 `work/photos-odengki/`로 내려받아 썼다. 픽스처 A의 기존 사진은 AI 연출로 보여 쓰지 않았다.
- 사진은 앱의 "지금 사진 찍기" 버튼이 여는 파일 선택 창에 넣었다(앱은 `camera_verified`로 저장).
  파일 수정 시각 등은 조작하지 않았다.

## 테스트용으로 생성한 데이터 (운영 DB)

| 무엇 | 내용 |
|---|---|
| 챌린지 | "같이 하면 한다 챌린지" (방장 A · setup · 2026-09-15~10-12 · 공개 모집 꺼짐 · 사진 인증 필수) + 참가 2 · 목표 2 · 동의 2 |
| 운동 | A: 트레드밀 3.6km/34분 + 벤치프레스 60kg×10·55→60kg×10 (19:31~20:05, 사진 1) · B: 벤치프레스 60kg×10 ×2 (20:07~20:09, 사진 1) |
| 사진 | `workout-images` 버킷에 A·B 각 1장 |
| 응원 | B→A 🔥 1회(리허설), A→B 💪 1회 |
| 좋아요 | B→A 1(리허설), A→B 1 |
| 기분 | A "🔥 컨디션 좋았다" |
| 댓글 (2026-09-15) | B의 9/14 운동에 A 댓글 "60kg 성공 축하해요" · B 답글 "덕분에 끝까지 했어요" + 댓글·답글 알림(픽스처끼리) |
| 알림 (픽스처끼리) | 응원·반응·운동 시작·기록 갱신 |
| ⚠️ **알림 (픽스처가 아닌 계정)** | **오뎅끼데스까(운영자 본인)**: 운동 시작 2 · 기록 갱신 1 / **test**: 운동 시작 1 · 기록 갱신 1 — A·B와 크루로 연결돼 있어 앱이 자동으로 보냈다. 푸시도 갔을 수 있다. 지우지 않았다(삭제는 승인 필요) |
| 분석 이벤트 | `analytics_events` 등 트래커 행 — `@gnd.local`이라 통계에서 빠진다 |

## XP — 이번 녹화에서 안 나온 이유

XP는 **그날 첫 "유효" 운동**에만 준다(`complete_workout_v2` → `is_valid_workout`).
웨이트는 **완료 세트 3개 이상**, 유산소는 거리·시간 기록 1세트 이상이어야 유효하다.
B는 2세트만 해서 무효 → XP 0 → XP 창이 뜨지 않았다(버그 아님, 악용 방지 규칙).
**무효 운동은 그날 XP 기회를 쓰지 않는다** — `record.mjs`는 이제 3세트로 준비한다.

## 발견한 문제

1. **"지금 바로 시작하기"를 눌러도 오늘 운동이 집계되지 않는다** [미검증 — 코드·DB 기준, 화면 재현 안 함]
   - 증상: 챌린지 시작일은 내일부터만 고를 수 있다. 방장이 "지금 바로 시작하기"를 누르면 토스트가
     "🏁 챌린지 시작! 오늘부터 기록이 반영돼요"라고 하지만, 오늘 한 운동은 반영되지 않는다.
   - 근거: 운영 DB `start_challenge`는 `status`만 `active`로 바꾸고 `start_date`는 그대로 둔다.
     `src/lib/challenge.ts:962`가 시작일 이전 날짜를 버린다. `src/lib/domain/challenge-invite.ts`의 주석
     ("날짜와 무관하게 오늘 시작한다")도 실제와 다르다.
   - 재현: 챌린지를 만들고(시작일 내일) 전원 목표·동의 → 지금 바로 시작하기 → 오늘 운동 완료 → 진행률 0%.
   - 영향: 일찍 시작한 챌린지의 첫날 기록이 빠지고, 안내 문구와 실제가 다르다.
   - 수정: **안 함**(기획안이 DB 변경·기능 수정을 금지).
2. **알림 본문에 내부 코드값이 그대로 보인다** (화면 확인)
   - 증상: 알림함에 "근육은퇴근중님이 내 운동에 반응했어요 / **like**", "응원 📣 / **fire**·**power**".
   - 영향: 사용자에게 영어 코드가 노출된다(영상 14번 장면에도 보인다).
   - 수정: 안 함.
3. **운동을 시작·완료하면 크루 전원에게 자동 알림이 간다** — 픽스처가 실사용자와 크루로 연결돼 있으면
   데모 촬영이 실사용자 알림을 만든다. 버그라기보다 픽스처 구성 문제다.
4. (참고) 모든 화면에서 `analytics_events` POST가 **409**를 낸다. 화면에는 영향 없음 [미검증 — 원인 미조사].

## 다시 녹화하는 방법

```bash
cd tools/demo-video
npm install                      # playwright-core 1.62.1 (브라우저는 ms-playwright의 chromium-1234 재사용)
node login.mjs A                 # 창에서 사람이 로그인 (창 아래 파란 띠)
node login.mjs B                 # 〃 (빨간 띠)
node record.mjs prep             # B의 오늘 계획을 벤치프레스 55·60·60kg × 10으로 준비
node record.mjs                  # 녹화 → work/rec/<시각>/
node bursts.mjs work/rec/<시각> B m01-home m02-feed-tap ...   # 장면별 화면 변화 구간 → edit-plan.json 오프셋 조정
node thumbs.mjs work/rec/<시각> B "m01-home+1,m12-challenge+1" work/sheet.png   # 대표 프레임 확인
node edit.mjs work/rec/<시각>              # 본편 2개 + 원본 2개 (원본은 수십 분 걸린다)
node edit.mjs work/rec/<시각> --no-raw     # 본편만
node edit.mjs work/rec/<시각> --no-raw --plan edit-plan-easy.json   # 쉬운 판 (카드 자막·느린 템포)

# 손가락·효과음을 쓰는 계획은 먼저
node finger.mjs                                   # work/finger.png (윈도우 컬러 이모지 👆, 다운로드 없음)
node sfx.mjs                                      # work/sfx/{chime,complete,rise}.wav (ffmpeg 합성)
node taps.mjs work/rec/<시각> edit-plan-first-contact.json      # (저장소 루트에서) 탭 위치 → taps-first-contact.json
node edit.mjs work/rec/<시각> --no-raw --plan edit-plan-first-contact.json   # 첫 접촉용 50초 v1 (알약 자막)
node edit.mjs work/rec/<시각> --no-raw --plan edit-plan-first-contact-v2.json   # 첫 접촉용 50초 v2 (카드 자막, 같은 탭 파일)
node taps.mjs tools/demo-video/work/rec/comment-20260915 taps-plan-comment.json   # (저장소 루트에서) 댓글 녹화 탭
node edit.mjs work/rec/final-20260914 --no-raw --plan edit-plan-first-contact-shorts.json   # 쇼츠판 (댓글 녹화를 runs로 함께 씀)
```

**댓글 녹화 (2026-09-15, `comment-20260915`)** — `RUN=comment-20260915 node driver.mjs`로 A·B를 띄우고 한 단계씩:
A 피드 → B의 9/14 게시물 💬 → 스크롤 → "60kg 성공 축하해요" 입력 → 등록 / B 🔔 → "헬스장주주님이 내 운동에 댓글을 남겼어요" → 게시물(댓글 펼침) → 답글 달기 → "덕분에 끝까지 했어요" → 등록.
운영 DB에 생긴 것: 픽스처 A의 댓글 1 · 픽스처 B의 답글 1 · 그에 따른 B·A 알림(픽스처끼리). 게시물 시각은 "9시간 전"으로 보인다.
⚠️ 알림 목록에 기존 이슈(반응·응원 알림 본문이 `like`·`power` 코드로 보임)가 잠깐 비친다.

⚠️ **`record.mjs`는 [미검증]이다.** 2026-09-14 영상은 `driver.mjs`(브라우저를 띄워 둔 채 한 단계씩 명령)로
화면을 확인하며 찍었고, `record.mjs`는 그때 통과한 선택자·순서를 옮겨 적은 것이다. 한 번에 끝까지 돌려 본
적은 없다. 처음엔 조종기로 단계를 확인하며 돌려라:

```bash
RUN=myrun node driver.mjs                      # 백그라운드로 켜 둔다 (A·B 1080×1920 헤드리스)
node drive.mjs A '[{"rec":"start"},{"goto":"/challenge"},{"mark":"c02-a-add-tap"},{"shot":"x"},{"snap":true}]'
```

재녹화 전 확인할 것
- **B가 그날 운동하지 않았는지**(XP 창은 첫 유효 운동에만), 새 챌린지 이름이 이미 있지 않은지.
- 진행률 장면용 챌린지(`DEMO_PROGRESS_CHALLENGE`, 기본 "9월 4주 챌린지")가 아직 진행 중인지 — 9월 26일에 끝난다.
- 피드 맨 아래·크루 목록에 실사용자가 비치지 않는지(`thumbs.mjs`로 전 장면 확인).

## 편집 계획 파일에서 쓸 수 있는 것

| 키 | 뜻 |
|---|---|
| `name` | 산출물 이름 (`artifacts/<name>.mp4`, `<name>-nocaption.mp4`) |
| `layout: "band"` | 앱 화면을 86%(928×1650)로 줄여 위에 두고 아래 띠에 자막 — **앱 UI를 가리지 않는다** |
| `layout: "card"` | 둥근 앱 화면(81%) + 아래 **흰 카드**(골드 테두리·그림자)에 검은 자막 — band는 "자막인지 화면인지 구분이 안 간다"(사용자) |
| `layout: "clean"` | 둥근 앱 화면 **90%** + 아래 작은 **알약 자막**(흰 바탕·골드 테두리·그림자) — 첫 접촉용, 흰 설명판이 "사용 설명 영상 같다"는 지침 |
| `layout: "shorts"` | 유튜브 쇼츠 UI를 피한 배치 — 자막 카드 위(290~480), 앱 화면 790×1404 아래(505~). 마지막 문구도 가운데보다 위 |
| `runs: {이름: {dir, tapsFile}}` + 클립 `run` | 다른 녹화 폴더의 장면을 섞는다 (댓글 장면). 탭 파일도 그 폴더 것을 쓴다 |
| `headline` | clean: 알약 위 작은 골드 한 줄 (첫 6초 "팔로워가 혼자 운동하지 않게 만드는 챌린지") |
| `finger` / `tapsFile` / `taps[]` | 탭 위치에 👆 손가락(누르기 0.45초 전 다가와 살짝 누름). 탭은 `taps.mjs`가 계획의 창에서 찾아 계획별 파일에 쓴다 |
| `sfx: [{at, type, volume}]` | 클립 시작 기준 at초에 효과음(`chime`·`complete`·`rise`) |
| `music: {file, volume, fadeIn, fadeOut}` | 배경음. 첫 접촉판은 0.28(음악만 구간 평균 약 -34dB) |
| `nocaption: false` | 자막 없는 판을 만들지 않는다 |
| `ending.cta` | GND Beta 아래 한 줄 (clean, 또는 cta가 있는 card) |
| `ending.text`만 (sub·cta 없음) | 문구만 줄마다 가운데 정렬(`\n`으로 줄 나눔, `ending.size` 기본 60) — 쉬운 판·첫 접촉 v2 |
| `capSize` / `subSize` | card: 제목(기본 54)·설명(기본 38) 글자 크기 — 긴 제목은 44로 줄인다 |

⚠️ **완성본 보호:** `artifacts/<name>.mp4`가 이미 있으면 `edit.mjs`가 멈춘다. 새 버전은 계획의 `name`을 바꿔 만든다(덮어쓰기는 `--overwrite`).
⚠️ 배경음 `work/music/mixkit-i-do-1001.mp3` — Mixkit "I Do!"(Michael Ramir C.), Mixkit Stock Music Free License(상업 영상 사용·출처 표기 불필요, 음원 단독 재배포 금지). 사용자 승인 후 다운로드, `work/music/LICENSE-NOTE.txt`. YouTube Content ID 여부는 공식 문서에 없어 [미검증].
| `from`/`to`/`offset`/`toOffset`/`dur` | 장면 표시 기준 구간(초) |
| `skip: [[s,e]]` | 클립 시작 기준 구간을 잘라 냄(로딩 화면) |
| `speed` | 0.5 = 두 배 느리게 |
| `hold` | 클립 끝 화면을 N초 멈춤(결과 숫자 읽을 시간) |
| `caption` / `sub` / `capSize` | 띠 배치: 노란 제목 / 흰 설명 / 제목 글자 크기 |

⚠️ 자막 글꼴(Noto Sans KR)은 **이모지와 ✓를 못 그린다**(네모로 나온다). ①~⑳·→·×·%는 된다.
⚠️ 처음 보는 사람 기준으로 만든다 — 사용자 지시 "너무 빨라서 한 번에 이해 못 한다, 초등학생도 알 수 있게"(2026-09-14).

## 만들면서 겪은 함정 (다시 밟지 말 것)

- 헤드리스 screencast는 기기 배율 에뮬레이션만으로 **405×720**을 준다 → `--force-device-scale-factor`를 같이 준다(`lib.mjs`).
- screencast 프레임 간격(0.014~0.43초)을 ffmpeg concat에 그대로 넘기면 클립 길이가 ±2.7초 어긋나고,
  긴 정지 프레임을 한 줄 `duration`으로 두면 **fps 필터가 맨 앞 프레임을 버린다** → 1/30초마다 한 줄씩 풀어 쓴다(`screencast.mjs`).
- A·B 로그인 창을 같은 자리에 띄우면 겹쳐서 **B 프로필에 A가 로그인**된다 → 위치를 가르고 이름표를 단다(`login.mjs`).
- 이 폴더의 `.profiles/`·`work/`가 gitignore에서 빠지면 **Tailwind가 그 파일들을 훑어 CSS가 깨진다**(2026-09-14 `.next` 백업으로 실제로 겪음).
- Git Bash에서 `/home` 같은 인자는 윈도우 경로로 바뀐다 → `MSYS_NO_PATHCONV=1`.
